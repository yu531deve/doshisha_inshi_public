# CLAUDE.md — 数学（math）担当エージェント作業規約

同志社大学大学院 生命医科学研究科 医工学コース 院試。**あなたはこの `01_数学/` 科目だけを担当する。**
他科目（`01_数学`/`02_力学`/`03_制御工学`/`04_材料力学` のうち自分以外）のディレクトリは**読み書き禁止**。
リポジトリ全体の理念は最上位 `../README.md` を参照。

## 最優先ルール（混入禁止）
- `参考/` に置く**東大版の解答は「体裁・粒度の手本」としてのみ使う。**
  東大の分野分類・問題内容・命名・データ出所を本プロジェクトに**持ち込まない**。
- 本科目の分野分類は、転記済み過去問（`01_exams/`）が揃ってから自分で作る（`05_meta/`）。
  先行大学の前提を流用しない（仕様書§0・§10）。

## ディレクトリ（この科目内で完結）
```
01_数学/
├ 00_common/preamble.sty      共通プリアンブル（読み取り専用扱い）
├ 00_common/verify_template.py 検算雛形（各年度へコピーして使う）
├ 01_exams/<年度>/            過去問原本PDF + 転記tex。解答は置かない
├ 02_solutions/<年度>/        解答例 tex/pdf + verify.py（★検算は解答の隣）
├ 03_augmentation/            AI生成の類似問題（型の水増し）
├ 04_notes/                   科目ノート（型の言語化）
├ 05_meta/                    分野マップ・学習ログ（転記後に作る）
├ 参考/                       体裁の手本（東大版。体裁のみ参照）
├ build/                      latexmk 中間物
└ original/                   元スキャン等の原本
```

## 解答作成ワークフロー（仕様書§4、厳守）
1. **読解**: スキャンは `pdftoppm -png -r 150`（不鮮明なら `-r 300`）で画像化し全小問を読む。
   要約を出して誤読を自己点検。判読困難な箇所は推測で埋めず `rem`（注意）環境で明記。
2. **検証ファースト**: `00_common/verify_template.py` を `02_solutions/<年度>/verify.py` にコピーし、
   全小問を検算。**全 PASS（または SKIP に理由）になるまで tex を書き始めない。**
   - この科目の検算方針: 数学はほぼ全て検算可能。SymPy で記号計算（積分・微分・行列・固有値・級数・ODE 等）、mpmath で高精度数値照合。最終解答は必ず `check()` で突き合わせ、全 PASS にしてから tex を書く。証明問題は具体値の代入で整合チェック（`check()` で個別ケースを確認）。
3. **執筆**: `02_solutions/<年度>/doshisha_bme_<年度>_math.tex` を作成。`00_common/preamble.sty` を使い、
   体裁・粒度は `参考/` のPDFに揃える（前提整理 → 小問 → Step分割 → 最終解答は `\ans{}` で boxed）。
4. **コンパイル**: 年度フォルダ内で `xelatex` を2回。警告・はみ出しゼロまで直す。
5. **目視確認**: 生成PDFを1ページずつ画像化し、数式崩れ・はみ出し・図の不備を確認。
6. **クロスチェック**: verify.py の値と PDF 中の boxed 解答が一致するか突き合わせ。

## 粒度（仕様書§6 / `参考/` と同一に）
- 行間を埋める（「明らか」「同様に」で飛ばさない）。最終解答は必ず boxed。
- 検算を本文に最低1箇所/大問（「検算:…✓」）。場合分け・境界・符号条件を明示。
- 文体は日本語の常体（「である」調）。

## 命名規則
- 解答 tex: `02_solutions/<年度>/doshisha_bme_<年度>_math.tex`（年度=AY表記）
- 検算:     `02_solutions/<年度>/verify.py`（解答と同居。トップに集約しない）
- 類似問題: `03_augmentation/aug_<連番>_<短い英語名>.tex`

## 当面の進め方
- **初回は1年度のみ**仕上げて粒度を確定する（仕様書「段階的に刻む」）。
- 1年度を仕上げてコミット（`solutions: AY<年度> 数学`）した区切りで止める。
- HELDOUT は設けない（全年度が作成対象）。
